backend:
  name: git-gateway
  branch: master

media_folder: "static/img/uploads" # Media files will be stored in the repo under static/images/uploads
public_folder: "/img/uploads"

collections:
  - name: "c"
    label: "C"
    folder: "content/c"
    create: true
    slug: "{{weight}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Draft", name: "draft", widget: "boolean", default: false }
      - {
          label: "Arguments",
          name: "arguments",
          widget: "list",
          required: false,
        }
      - {
          label: "Weight",
          name: "weight",
          widget: "number",
          valueType: "int",
          min: 1,
        }
      - { label: "Body", name: "body", widget: "markdown" }
  - name: "html"
    label: "HTML"
    folder: "content/hmtl"
    create: true
    slug: "{{weight}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Draft", name: "draft", widget: "boolean", default: false }
      - {
          label: "Arguments",
          name: "arguments",
          widget: "list",
          required: false,
        }
      - {
          label: "Weight",
          name: "weight",
          widget: "number",
          valueType: "int",
          min: 1,
        }
      - { label: "Body", name: "body", widget: "markdown" }
  - name: "css"
    label: "CSS"
    folder: "content/css"
    create: true
    slug: "{{weight}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Draft", name: "draft", widget: "boolean", default: false }
      - {
          label: "Arguments",
          name: "arguments",
          widget: "list",
          required: false,
        }
      - {
          label: "Weight",
          name: "weight",
          widget: "number",
          valueType: "int",
          min: 1,
        }
      - { label: "Body", name: "body", widget: "markdown" }
  - name: "hugo"
    label: "Hugo"
    folder: "content/hugo"
    create: true
    slug: "{{weight}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Draft", name: "draft", widget: "boolean", default: false }
      - {
          label: "Arguments",
          name: "arguments",
          widget: "list",
          required: false,
        }
      - {
          label: "Weight",
          name: "weight",
          widget: "number",
          valueType: "int",
          min: 1,
        }
      - { label: "Body", name: "body", widget: "markdown" }
  - name: "python"
    label: "Python"
    folder: "content/python"
    create: false
    slug: "{{weight}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Draft", name: "draft", widget: "boolean", default: false }
      - {
          label: "Arguments",
          name: "arguments",
          widget: "list",
          required: false,
        }
      - {
          label: "Weight",
          name: "weight",
          widget: "number",
          valueType: "int",
          min: 1,
        }
      - { label: "Body", name: "body", widget: "markdown" }
  - name: "git"
    label: "Git"
    folder: "content/git"
    create: true
    slug: "{{weight}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Draft", name: "draft", widget: "boolean", default: false }
      - {
          label: "Arguments",
          name: "arguments",
          widget: "list",
          required: false,
        }
      - {
          label: "Weight",
          name: "weight",
          widget: "number",
          valueType: "int",
          min: 1,
        }
      - { label: "Body", name: "body", widget: "markdown" }
  - name: "ai"
    label: "Artificial Intelligence"
    folder: "content/ai"
    create: true
    slug: "{{weight}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Draft", name: "draft", widget: "boolean", default: false }
      - {
          label: "Arguments",
          name: "arguments",
          widget: "list",
          required: false,
        }
      - {
          label: "Weight",
          name: "weight",
          widget: "number",
          valueType: "int",
          min: 1,
        }
      - { label: "Body", name: "body", widget: "markdown" }
  - name: "rpi"
    label: "Raspberry Pi"
    folder: "content/rpi"
    create: true
    slug: "{{weight}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Draft", name: "draft", widget: "boolean", default: false }
      - {
          label: "Arguments",
          name: "arguments",
          widget: "list",
          required: false,
        }
      - {
          label: "Weight",
          name: "weight",
          widget: "number",
          valueType: "int",
          min: 1,
        }
      - { label: "Body", name: "body", widget: "markdown" }
  - name: "arduino"
    label: "Arduino"
    folder: "content/arduino"
    create: true
    slug: "{{weight}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Draft", name: "draft", widget: "boolean", default: false }
      - {
          label: "Arguments",
          name: "arguments",
          widget: "list",
          required: false,
        }
      - {
          label: "Weight",
          name: "weight",
          widget: "number",
          valueType: "int",
          min: 1,
        }
      - { label: "Body", name: "body", widget: "markdown" }
  - name: "linux"
    label: "Linux"
    folder: "content/linux"
    create: true
    slug: "{{weight}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Draft", name: "draft", widget: "boolean", default: false }
      - {
          label: "Arguments",
          name: "arguments",
          widget: "list",
          required: false,
        }
      - {
          label: "Weight",
          name: "weight",
          widget: "number",
          valueType: "int",
          min: 1,
        }
      - { label: "Body", name: "body", widget: "markdown" }
  - name: "bash"
    label: "Bash"
    folder: "content/bash"
    create: true
    slug: "{{weight}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Draft", name: "draft", widget: "boolean", default: false }
      - {
          label: "Arguments",
          name: "arguments",
          widget: "list",
          required: false,
        }
      - {
          label: "Weight",
          name: "weight",
          widget: "number",
          valueType: "int",
          min: 1,
        }
      - { label: "Body", name: "body", widget: "markdown" }
  - name: "electronics"
    label: "Electronics"
    folder: "content/electronics"
    create: true
    slug: "{{weight}}-{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Draft", name: "draft", widget: "boolean", default: false }
      - {
          label: "Arguments",
          name: "arguments",
          widget: "list",
          required: false,
        }
      - {
          label: "Weight",
          name: "weight",
          widget: "number",
          valueType: "int",
          min: 1,
        }
      - { label: "Body", name: "body", widget: "markdown" }
